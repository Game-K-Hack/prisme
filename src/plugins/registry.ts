import type { FranceDataPlugin } from '@/plugins/index'

/**
 * Catalogue des plugins built-in disponibles dans Prisme.
 * Le plugin "Prix Carburants" est désormais distribué en
 * tant que plugin externe (.prm) — voir examples/prix-carburants/
 */
export const PLUGIN_CATALOG: FranceDataPlugin[] = [

]
