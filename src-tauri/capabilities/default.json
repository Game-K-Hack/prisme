{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions par défaut de la fenêtre principale Prisme",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "http:default"
  ]
}
